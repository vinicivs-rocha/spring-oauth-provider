spring:
  data:
    mongodb:
      uri: mongodb://oauth:123456@localhost:27017/oauth?authSource=admin
      user: oauth
      password: "123456"
jwt:
  secret: "jwt-tokens-that-should-be-changed-production"
  issuer: "jwt-issuer"
logging:
  level:
    org:
      springframework:
        security:
          - DEBUG
          - TRACE